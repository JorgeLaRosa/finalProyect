import { HeadersSizeDirective } from './headers-size.directive';

describe('HeadersSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadersSizeDirective();
    expect(directive).toBeTruthy();
  });
});
